# Port the NestJS server listens on
PORT=3000

# MongoDB connection string (Atlas or local instance)
DATABASE_CONNECTION_STRING=mongodb://localhost:27017/poly-arena

# Firebase Admin service account, base64-encoded JSON.
# Generate one in the Firebase console (Project settings > Service accounts),
# then encode it: base64 -i serviceAccount.json
FIREBASE_SERVICE_ACCOUNT=
