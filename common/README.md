# common

Shared TypeScript definitions used by both the Angular client and the NestJS server:
interfaces, enums, constants, gateway event names, and utility types.

Keep this folder free of application logic — it exists so that client and server
always agree on the shape of the data they exchange.
